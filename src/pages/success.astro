---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Mensaje enviado`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Éxito</span>
          <span class="flex justify-center text-green-600 dark:text-green-500">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="icon icon-tabler icon-tabler-circle-check"
              width="128"
              height="128"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
              fill="none"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path stroke="none" d="M0 0h24v24H0z" fill="none"></path>
              <circle cx="12" cy="12" r="9"></circle>
              <path d="M9 12l2 2l4 -4"></path>
            </svg>
          </span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">¡Mensaje enviado!</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Gracias por contactarme. Te responderé a la mayor brevedad posible.
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Volver al inicio</a>
      </div>
    </div>
  </section>
</Layout>
